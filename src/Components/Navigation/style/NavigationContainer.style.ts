import styled from 'styled-components';

// colors
import { darkGrey } from '../../../style/colors.style';

const NavigationContainer = styled.header`
  display: flex;
  justify-content: center;
  background-color: ${darkGrey};
`;

export default NavigationContainer;
